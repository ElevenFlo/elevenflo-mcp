# Cursor

Use the **Add to Cursor** button above. It opens Cursor with `https://elevenflo.com/mcp` pre-filled. Click **Install** and approve OAuth on the next screen.

Manual path: **Cursor → Settings → MCP → Add new server**, pick HTTP transport, and paste:

```text
https://elevenflo.com/mcp
```

Smoke test:

```text
Use the elevenflo MCP tool search_cases to find the Chapter 11 case FTX and return the top match.
```
