# Changelog

## 0.2.0 - 2026-06-10

- Synced `docs/` wholesale from the ElevenFlo monorepo customer bundle:
  added the workflow recipe pages (`workflows.md` plus six `workflow-*.md`
  files), renamed `request-access.md` to `access.md`, and removed
  `examples.md`, `registry.md`, `security.md`, and `setup.md` (superseded by
  the recipes, the tool catalog, the permissions page, and the root README).
- Replaced the root `llms.txt` with the live site copy. This corrects the
  tool-set description: the hosted tool set is read-only and does not include
  case event search or case-law metadata lookup.
- Rewrote the root README access and tool-set language to match the live
  docs: ElevenFlo MCP is included with Pro and provisioned automatically.

## 0.1.0 - 2026-05-01

- Created the public `ElevenFlo/elevenflo-mcp` documentation and metadata
  package.
- Added official MCP Registry metadata for `com.elevenflo/mcp`.
- Published public setup docs, tool catalog, security posture, examples,
  troubleshooting, support, and `llms.txt`.
